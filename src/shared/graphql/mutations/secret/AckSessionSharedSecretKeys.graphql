mutation AckSessionSharedSecretKeys(
  $chatId: String!
  $secretSessionId: String!
  $sharedKeyIds: [String!]!
) {
  ackSessionSharedSecretKeys(
    chatId: $chatId
    secretSessionId: $secretSessionId
    sharedKeyIds: $sharedKeyIds
  )
}
