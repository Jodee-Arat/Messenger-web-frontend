mutation AckSessionSecretMessages(
  $chatId: String!
  $secretSessionId: String!
  $messageIds: [String!]!
) {
  ackSessionSecretMessages(
    chatId: $chatId
    secretSessionId: $secretSessionId
    messageIds: $messageIds
  )
}
