mutation RefreshSecretSession(
  $secretSessionId: String!
  $publicPreKey: PreKeyInput!
) {
  refreshSecretSession(
    secretSessionId: $secretSessionId
    publicPreKey: $publicPreKey
  ) {
    id
    userId
    platform
    deviceName
    createdAt
    expiresAt
    revokedAt
  }
}
