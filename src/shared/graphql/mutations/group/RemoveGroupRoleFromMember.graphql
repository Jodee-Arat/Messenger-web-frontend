mutation RemoveGroupRoleFromMember(
  $groupId: String!
  $roleId: String!
  $memberId: String!
) {
  removeGroupRoleFromMember(
    groupId: $groupId
    roleId: $roleId
    memberId: $memberId
  )
}
