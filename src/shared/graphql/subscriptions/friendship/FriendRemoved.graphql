subscription FriendRemoved($userId: String!) {
  friendRemoved(userId: $userId) {
    id
    userId
    friendId
    status
    user {
      id
      username
      avatarUrl
    }
    friend {
      id
      username
      avatarUrl
    }
    createdAt
    updatedAt
  }
}
