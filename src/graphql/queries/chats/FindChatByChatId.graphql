query FindChatByChatId($chatId: String!) {
  findChatByChatId(chatId: $chatId) {
    chatName
    draftMessages {
      id
      text
      files {
        fileName
        fileFormat
        fileSize
        id
      }
      repliedToLinks {
        id
        repliedTo {
          id
          text
          files {
            fileName
            fileFormat
            fileSize
            id
          }
          user {
            username
            id
          }
        }
      }
    }
  }
}
